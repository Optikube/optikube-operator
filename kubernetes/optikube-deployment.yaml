apiVersion: apps/v1
kind: Deployment
metadata:
  name: optikube-deployment
  namespace: optikube-namespace
spec:
  replicas: 5
  selector:
    matchLabels:
      app: optikube
  template:
    metadata:
      labels:
        app: optikube
    spec:
      containers:
        - name: optikube4
          image: gcr.io/hardy-nation-412201/optikube4.5
          ports:
            - containerPort: 8080
          resources: 
            requests:
              cpu: "200m"
              memory: "400Mi"
            limits:
              cpu: "500m"
              memory: "600Mi"
      imagePullSecrets:
        - name: gcr-json-key
